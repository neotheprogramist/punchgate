# Punchgate — Environment Configuration
# Copy to .env and adjust values for your deployment.

# Path to the persistent identity file (Ed25519 keypair).
PUNCHGATE_IDENTITY=identity.key

# Multiaddr(s) to listen on (comma-separated for multiple).
PUNCHGATE_LISTEN=/ip4/0.0.0.0/tcp/4001

# Bootstrap peer multiaddr(s) to dial on startup (comma-separated for multiple).
PUNCHGATE_BOOTSTRAP=/ip4/127.0.0.1/tcp/4001/p2p/12D3KooWEXAMPLE

# Expose local service(s): name=host:port (comma-separated for multiple).
PUNCHGATE_EXPOSE=echo=127.0.0.1:7777

# Tunnel to remote service(s): peer_id:service_name@bind_addr (comma-separated for multiple).
PUNCHGATE_TUNNEL=12D3KooWEXAMPLE:echo@127.0.0.1:2222

# Tunnel by name via DHT discovery: service_name@bind_addr (comma-separated for multiple).
PUNCHGATE_TUNNEL_BY_NAME=echo@127.0.0.1:2222

# Override NAT status detection: "private" or "public".
PUNCHGATE_NAT_STATUS=private

# External address(es) for this node (comma-separated for multiple).
# Required for relay servers so clients learn how to reach the relay.
PUNCHGATE_EXTERNAL_ADDRESS=/ip4/203.0.113.1/tcp/4001
